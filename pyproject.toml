
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "strategy_optimizer"
version = "0.1"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.black]
line-length = 88
target-version = ['py311']

[tool.isort]
profile = "black"
